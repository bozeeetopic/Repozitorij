import { Plantdetailmodel } from './plantdetailmodel.model';

describe('Plantdetailmodel', () => {
  it('should create an instance', () => {
    expect(new Plantdetailmodel()).toBeTruthy();
  });
});
